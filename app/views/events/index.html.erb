<!-- This title and h1 will change depending on organization -->
<% provide(:title, "HSA Events") %>
<div class="container">

<%= render 'shared/event_form' %>

<h1> SAI Events</h1><style>h1 {text-align: center;}</style>
<%= will_paginate %>

<ul class="events">
<%= render @events %>
</ul>

<%= will_paginate %>
</div>